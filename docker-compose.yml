services:
  dnd-app:
    container_name: dnd-immersive-master
    build: .
    restart: always
    ports:
      - "8500:3000"
    volumes:
      - ./assets:/app/public/assets
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - NODE_ENV=production

